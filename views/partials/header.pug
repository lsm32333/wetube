header.header
    .header__wrapper
        .header__column
            a(href=routes.home)
                //- i.은 아이콘 의미. fontawesome.com에서 아이콘 가져오자
                i.fab.fa-youtube
        .header__column
            //- 이 form태그로 videoController.js 파일의 search 함수를 실행한다.
            form(action=routes.search, method="get")
                input(type="text", placeholder="Search for: term...", name="term")
        .header__column
            ul
                if !loggedUser
                    li
                        a(href=routes.join) Join
                        //- routes 아래의 변수들은 res.locals에 의해 global하게 설정 되었으므로, 여기서도 사용 가능!
                    li
                        a(href=routes.login) Log In
                else
                    li
                        a(href=`/videos${routes.upload}`) Upload
                    li
                        a(href=routes.me) Profile
                    li
                        a(href=routes.logout) Log Out